{
  "name": "@rei/components",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "peerDependencies": {
    "react": "~19.1.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/react": "~19.1.0",
    "react": "~19.1.0",
    "react-dom": "~19.1.0",
    "rimraf": "~6.0.0"
  }
}